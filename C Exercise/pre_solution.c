#include <stdio.h>#include <stdlib.h>// https://repl.it/repls/EveryHelplessTinamoustruct menu {	int no; // 메뉴번호	char name[20]; // 메뉴명	int price; // 가격};int add_menu(struct menu * m[], int c);void save_menu(struct menu * m[], int c);void show_menu(struct menu * m[], int c);main(){	struct menu * menupan[20];	int count = 0, job;	while (1)	{		printf("작업: 1.메뉴보기 2.메뉴추가 3.메뉴저장 99.종료");		scanf("%d", &job);		if (job == 1) show_menu(menupan, count);		else if (job == 2) count = add_menu(menupan, count);		else if (job == 3) save_menu(menupan, count);		else break;	}}int add_menu(struct menu * m[], int c){	struct menu *temp;	temp = (struct menu *)malloc(sizeof(struct menu));	// 1. 메뉴 정보를 입력받는다.	printf("메뉴번호, 이름, 가격 : ");	scanf("%d %s %d", &temp->no, temp->name, &temp->price);	m[c] = temp;	return c + 1;}void save_menu(struct menu * m[], int c){	int i;	FILE *file1;	file1 = fopen("menu.txt", "wt");	for (i = 0; i<c; i++) {		fprintf(file1, "%d %s %d\n", m[i]->no, m[i]->name, m[i]->price);	}	printf("파일저장 완료!\n");	fclose(file1);}void show_menu(struct menu * m[], int c){	int i;	printf("번호 이름 가격\n");	for (i = 0; i<c; i++) {		printf("%d %s %d\n", m[i]->no, m[i]->name, m[i]->price);	}}